<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:jpa="http://www.springframework.org/schema/data/jpa"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-3.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa-1.0.xsd">

	<jpa:repositories base-package="com.zenika.nordnet.repository" />
	
	<!-- TODO 09 le repository pour Address est déjà déclaré, faire en sorte qu'il soit utilisé par Spring Data JPA -->
	<!-- 
		NB : le repository custom pour Address ne suit pas les conventions de détection de Spring Data JPA
		(ni pour le nom de classe, ni pour le nom de bean)
	 -->
	<bean id="addressRepositoryCustom" class="com.zenika.nordnet.repository.JpaAddressRepositoryCustom" />
	
	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="entityManagerFactory" />
	</bean>

	<bean id="entityManagerFactory"
		class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
				<property name="generateDdl" value="true" />
				<property name="database" value="H2" />
			</bean>
		</property>
		<property name="packagesToScan" value="com.zenika.nordnet.model" />
	</bean>

	<jdbc:embedded-database id="dataSource" type="H2" />
	
	<jdbc:initialize-database data-source="dataSource">
		<jdbc:script location="/data.sql"/>
	</jdbc:initialize-database>

</beans>
